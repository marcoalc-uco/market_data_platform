# =============================================================================
# Environment Configuration Example
# Copy this file to .env and adjust values as needed
# =============================================================================
# This file works for both local development AND Docker Compose.
# Docker Compose will use the values from .env in the project root automatically.

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_NAME=Market Data Backend Platform
APP_VERSION=0.1.0
DEBUG=false

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
API_PREFIX=/api/v1

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Log format: "json" (production) or "console" (development)
LOG_FORMAT=console

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# For local development (Python running on host):
DB_HOST=localhost
DB_PORT=5432
DB_USER=market_data
DB_PASSWORD=market_data_pass
DB_NAME=market_data

# Alternative: Full connection URL (overrides individual params if set)
# DATABASE_URL=postgresql://user:password@localhost:5432/market_data

# Connection pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# Note: Docker Compose overrides DB_HOST internally to connect to the 'postgres' service.
# The DATABASE_URL is constructed in docker-compose.yml with the correct host.

# -----------------------------------------------------------------------------
# Docker-Specific Variables (used by docker-compose.yml)
# -----------------------------------------------------------------------------
# These variables are used by Docker Compose to configure containers.
# For local development, they are not used by the Python application directly.

# PostgreSQL container configuration
POSTGRES_USER=market_data
POSTGRES_PASSWORD=market_data_pass
POSTGRES_DB=market_data

# Grafana container configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# -----------------------------------------------------------------------------
# Auth / JWT Configuration
# -----------------------------------------------------------------------------
# Generate with: openssl rand -hex 32
SECRET_KEY=changeme-generate-with-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Admin credentials
ADMIN_EMAIL=admin@market.com
# Generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
ADMIN_PASSWORD_HASH=$2b$12$replacethiswitharealhash

# -----------------------------------------------------------------------------
# Scheduler Configuration
# -----------------------------------------------------------------------------
# Enable/disable automated data ingestion scheduler
SCHEDULER_ENABLED=true
# Interval in minutes between ingestion runs
INGESTION_INTERVAL_MINUTES=60
