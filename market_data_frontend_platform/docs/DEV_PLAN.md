# Frontend DEV_PLAN

## Active Tasks

- **[PENDING] Phase 1 — Vite Project Setup**
  - Dependency toolchain installation.

## Next Immediate Action Items

1. Run `npm create vite@latest . -- --template react` in `market_data_frontend_platform/`.
2. Install runtime: `@tanstack/react-query lightweight-charts react-router-dom`.
3. Install dev deps: vitest, RTL, eslint, prettier.
4. Configure aliases and `.eslintrc.cjs`.
5. Write `nginx.conf` and `Dockerfile`.
6. Add `.env.example`.
7. Verify toolchain checks (`npm run lint`, test, build).

## Blockers

- None. Awaiting bootstrap initialization.
