# No .env file is needed for Docker/production.
# Nginx proxies /api/ calls to the backend container internally.
#
# Only create a .env for local development outside Docker:
# VITE_API_URL=http://localhost:8000
