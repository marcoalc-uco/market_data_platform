# =============================================================================
# Frontend Environment — Market Data Frontend Platform
# =============================================================================
# This file is ONLY needed for local development OUTSIDE Docker.
# In Docker/production, Nginx proxies /api/ to the backend container directly.
#
# Copy to .env and adjust if your backend runs on a different host/port:

VITE_API_URL=http://localhost:8000
